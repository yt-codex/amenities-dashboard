name: Legacy Ops Probe Placeholder (Manual Only)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder notice
        run: |
          echo "This legacy workflow is manual-only and does not emit ops/probe.json."
          echo "Primary probe signal now comes from .github/workflows/data-refresh.yml."
